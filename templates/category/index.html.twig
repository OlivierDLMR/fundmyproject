{% extends 'base.html.twig' %}

{% block title %}Category index{% endblock %}

{% block body %}
    <div class="container">
        <h1>Catégorie : Film</h1>
        <p>Il y a 3 projets dans cette catégorie :</p>
        <section class="container">
            <h1>La liste de nos Catégories </h1>
        </section>
        <li class="nav-item dropdown">
            a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" id="download">Catégories <span class="caret"></span></a>
            <div class="dropdown-menu" aria-labelledby="download">
                <a class="dropdown-item" href="{% for category in categories %}
                   {% include 'category/_thumbail.html.twig' with {'category': category} %}
                {% endfor %}"></a>
            </div>
        </li>
    </div>
{% endblock %}
